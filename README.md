# Face Mesh Warp Project

This project uses MTCNN for face detection and MediaPipe for face mesh generation. MTCNN requires installation of tensorflow so highly recommand using a virtual enviroment.

## Installation
**Create a virtual environment** (optional but recommended):
   ```bash
   python3 -m venv myenv
  ```
Install packages
```bash
pip install -r requirements.txt
```
